# Script 01: Basic combat
# Tests: enter combat, play cards, block reduces damage, block expires, gold earned on win

# Enter floor 1 (louse)
enter

# Set up a known hand so card positions are deterministic
add strike
add defend
add bash

# Turn 1: defend (5 block), then strike (6 damage)
play 1 0  # wrong index — use the known add positions
e
